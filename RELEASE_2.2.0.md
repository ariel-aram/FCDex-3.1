## 2.2.0

- Unified **`/fcdex admin`** hub (Shop, Craft, Boss, Owners) — all ephemeral Components v2 panels
- **`/fcdex shop`** only (removed duplicate `/shop browse` group)
- Shop bundle items support optional **special** per clubball (migration `0010`)
- Guild **boss raids** inspired by BallsDex Boss Pack (join button, attack/defend rounds, damage standings)
- **`/fcdex boss`** player panel; boss admin under `/fcdex admin` → Boss
- **Craft admin** panel for SBC recipes without the web panel
- **Rarity** — Spawnable, Unspawnable, and **Specials** tabs

Install: `path = "fcdex_3_1"` · pin `@2.2.0` · migrate through `0010`.
