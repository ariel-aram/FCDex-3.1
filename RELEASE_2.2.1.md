## 2.2.1

- Boss raids capped at **3 rounds** — resolve each round before starting the next; conclude after round 3
- Boss admin: optional **reward clubball** (PK or country name) on start; **Start by PK / name** button
- Player `/fcdex boss` panel: **Join raid** during registration (fixes “0 fighters” when only using the command)
- Admin clubball fields accept **PK** (`42`, `#42`) or country name — shop bundles, SBC recipes, owners lookup, boss start
- Shared resolver: `fcdex_3_1.fcdex_ext.bd_resolve`

Install: `path = "fcdex_3_1"` · pin `@2.2.1`.
